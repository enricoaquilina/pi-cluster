---
profile: basic

exclude_paths:
  - .git/
  - .claude/
  - ansible/
  - secrets/

skip_list:
  - yaml[truthy]
  - name[missing]
  - name[casing]

warn_list:
  - command-instead-of-shell
  - no-changed-when
