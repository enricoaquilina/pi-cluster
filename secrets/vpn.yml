# Encrypt this file with: ansible-vault encrypt secrets/vpn.yml
# Then fill in the actual values from KeepSolid's WireGuard config page.
#
# Master (Belgium server)
vault_master_keepsolid_key: "REPLACE_WITH_BELGIUM_PRIVATE_KEY"

# Slave0 (Hong Kong server)
vault_slave0_keepsolid_key: "REPLACE_WITH_HK_PRIVATE_KEY"
